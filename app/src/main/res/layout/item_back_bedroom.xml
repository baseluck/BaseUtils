<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/tv_back_bedroom"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="15dp"
        android:layout_marginTop="20dp"
        android:textColor="@color/color333333"
        android:textSize="15sp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="假期类型" />

    <EditText
        android:id="@+id/et_back_bedroom"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:layout_marginEnd="30dp"
        android:layout_marginBottom="3dp"
        android:autofillHints=""
        android:background="@null"
        android:inputType="textMultiLine"
        android:maxLines="1"
        android:textColor="@color/color333333"
        android:textColorHint="@color/color999999"
        android:textSize="14sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="@+id/tv_back_bedroom"
        app:layout_constraintTop_toBottomOf="@+id/tv_back_bedroom"
        tools:ignore="LabelFor"
        tools:text="请选择" />


    <ImageView
        android:id="@+id/imageView"
        android:layout_width="wrap_content"
        android:layout_height="24dp"
        android:layout_marginEnd="15dp"
        android:contentDescription="@string/image"
        app:layout_constraintBottom_toBottomOf="@+id/et_back_bedroom"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/et_back_bedroom"
        app:srcCompat="@drawable/ic_keyboard_arrow_right" />
</androidx.constraintlayout.widget.ConstraintLayout>